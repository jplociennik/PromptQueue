# Historia przeglądów

- pq-1 (Fundament, 2026-07-15): AKCEPTUJ Z UWAGAMI. Implementacja wierna projektowi v3. Uwagi: (1) luka testowa Fail-z-terminala, (2) pinować major tag Postgresa. Reszta (Relational pin, mount PG18, .gitignore bin/obj) OK.
- pq-2 (API promptów + odczyt statusów, 2026-07-15): AKCEPTUJ. Wierna zaakceptowanemu projektowi v2 (13 elementów, ścieżki, piny pakietów). Build 0/0, 33/33 (Domain 16 + Api.UnitTests 10 + Api.IntegrationTests 7); data-layer nietknięty (0 migracji, słusznie). Zero uwag krytycznych; sugestie kosmetyczne: walidator nie short-circuituje przy nadmiarowej liczbie (pełny loop, nieszkodliwe przy limicie body), brak `<summary>` na publicznych helperach TestSupport (self-explanatory). Zweryfikowane bezpieczne: `prompts[0].Status`, DisposeAsync-hiding, determinizm testu kolejności, ścieżka 500.
- pq-2 refaktor testów (follow-up, 2026-07-15): AKCEPTUJ. Warstwa testów + rename `*.Tests`→`*.UnitTests` + AwesomeAssertions 9.4.0; produkcja nietknięta poza fazą GREEN w `Program.cs` (JSON enum converter, `MapPromptEndpoints`, `partial Program`). Dedup przez `PromptApiClient` + `record PostAndFetch` — realny, KISS, nie chowa statusu/nagłówków. AssertionScope poprawny (await przed scope, single-assert bez scope). 33/33 zachowane, `ThrowingPromptRepository`/500 OK, `.sln` bez martwych wpisów. 1 drobne OSTRZEŻENIE: `ProblemDetailsAssertions.cs:13` `GetProperty("errors")` rzuca przed scope → `TryGetProperty`. SUGESTIA: CPM (Directory.Packages.props) — 4 projekty testowe dublują te same piny.
- pq-3 (Worker + Ollama, 2026-07-15): AKCEPTUJ Z UWAGAMI. Wierna zaakceptowanemu projektowi v2 (wszystkie 15 elementów, ścieżki, piny; K1–K5 obecne). Build 0/0, 51/51 (Domain 20 + Api.UnitTests 10 + Api.IntegrationTests 7 + Worker.UnitTests 10 + Worker.IntegrationTests 4). 0 migracji — poprawnie (brak zmiany schematu). Podwójny-OCE/retry/pusta-odpowiedź/scope-per-cykl/K1/fail-fast/compose — wszystko zgodne; oba odstępstwa (eskalacja WARN→ERROR = mandat v2; healthcheck `ollama list`) uzasadnione. 1 OSTRZEŻENIE: guard per-prompt w `ProcessPendingAsync` łyka też `SaveChanges`/DbException → potencjalny tight-loop (pierwszy save pada) lub osierocenie w Processing (finalny save pada) — niskie prawdopodobieństwo, fix = zawęzić guard, DB niech bąbelkuje do `RunInScopeAsync`. SUGESTIE: CPM/`Directory.Build.props`; TestSupport→Api coupling ciągnie Api tranzytywnie do Worker.UnitTests (PromptBuilder potrzebuje tylko Domain).
- pq-4 (Frontend: setup + dodawanie promptów, 2026-07-15): AKCEPTUJ Z UWAGAMI. Fundament FE wierny projektowi v2 co do struktury (reguła 9) i kontraktu (lustro pq-2, zweryfikowane vs `PromptContracts.cs`). Build 0, testy 11/11 (toApiError 3, promptFieldsReducer+isBlank 6, useCreatePrompts 2). Reset-po-sukcesie + blokada double-submit + `aria-label` OBECNE i poprawne; dev-CORS OK; brak `any`; brak barreli; hooki płasko w `hooks/`. 3 OSTRZEŻENIA: (a) usunięcie ostatniego pola→pusty POST→400; (b) `validationErrors` niewyświetlane (Alert=tylko generyczny EN title); (c) HTTP=axios = ODSTĘPSTWO od doc (fetch/apiFetch) — zsynchronizować doc. SUGESTIE: `satisfies CreatePromptsRequest`, plural "promptów", test fallback non-ApiError, RTL cleanup przy globals:false, aria-label na "Usuń".
- pq-6 (Orkiestracja docker-compose + README, 2026-07-15): AKCEPTUJ. Implementacja 1:1 z projektem v2, zweryfikowana LIVE przez usera (`docker compose up`, 3 obrazy, wszystkie healthy, DoD e2e przez Playwright: pending→processing→completed z realną odpowiedzią llama3.2; ścieżka błędu Failed przy stopie ollama, Worker żyje). 6 usług compose (postgres:18-alpine, ollama:0.32.0, ollama-pull one-shot, api, worker, frontend), Dockerfile'e multi-stage (SDK→aspnet+curl dla Api, SDK→runtime dla Worker, node:20→nginx:1.27 dla frontu), nginx SPA fallback + proxy `/api`→api:8080, `.env.example` (dev-creds postgres/postgres jako `${VAR:-default}`, `.env` gitignored — brak realnego sekretu). FIX precedencji: `AddEnvironmentVariables()` PO ręcznym `AddJsonFile` w obu Program.cs — konieczny bo współdzielony `backend/appsettings.json` ma zaszyte `Worker:OllamaBaseUrl=http://localhost:11434`, które inaczej nadpisywałoby env compose. `.dockerignore` backend (`**/bin`,`**/obj`,`**/*.user`) — bez tego `publish --no-restore` pada na hostowe obj/. Zero migracji (poprawnie). SUGESTIE (kosmetyka, nie blokują): (1) `.dockerignore` mógłby dodać `.vs/` (kontekst=./backend, tam leży .sln); (2) README dev-block ```bash zawiera PowerShell `$env:` (niedostępne na linux/mac — dodać `export`); (3) README l.82 referuje user-specific `. $PROFILE...all-test` — nieprzenośne dla innych devów (dać `dotnet test`); (4) README bez ostrzeżenia o zmianie dev-hasła na prod; (5) nginx bez gzip/security-headers (nice-to-have); (6) frontend bez `restart:` (projekt świadomie tylko api/worker). Efekt uboczny FIX-a (negligible): manualny AddJsonFile teraz cieniuje command-line args dla configu — nikt tu nie używa args.
